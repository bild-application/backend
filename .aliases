### To use aliases, run `source .aliases`


## IMPORTANT ALIASES ##

    # Docker Compose: "dc"
        alias dc='docker compose'

    # Docker Exec: "de"
        alias de='docker exec -ti backend-php-1'

    # Php Console: "pc"
        alias pc='docker exec -ti backend-php-1 php bin/console'

    # Stan: "stan"
        alias stan='docker exec -ti backend-php-1 vendor/bin/phpstan analyse --level=6 --memory-limit=-1 src'


## DOCKER ALIASES ##

    # Docker Up: "du"
        alias du='docker compose up -d --wait'

    # Docker Build: "db"
        alias db='docker compose up -d --build'

    # Docker Build no Cache: "dbc"
        alias dbc='docker compose build --no-cache'
